import styled from "@emotion/styled";
import React from "react";

const Condition = () => {
  const Condition = styled.h3`
    font-size: 1.2rem;
  `;
  return <Condition>Clouds</Condition>;
};

export default Condition;
